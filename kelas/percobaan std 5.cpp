#include <iostream>
using namespace std;

struct node
{
    int data;
    node *next;
}; 
void addFirst {node *&head, node *&tail, int data} {
    node *nodebaru = new node;

    nodeBaru next = dataBaru;
    nodeBaru next = head;

    head = nodeBaru;

    if (tail == nullptr) {
        tail = nodeBaru;
    }
}

